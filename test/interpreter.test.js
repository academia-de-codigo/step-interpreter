import { expect } from 'chai';

describe('interpreter', function() {
    describe('context tests', function() {
        it('1 should be 1', function() {
            expect(1).to.equal(1, 'its equal');
        });
    });
});
